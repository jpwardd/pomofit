import { View, Text } from 'react-native'
import React from 'react'
import ListScreen from '../../../components/lists/ListScreen';

type Props = {}

const ListTab = (props: Props) => {
  return (
    <ListScreen />
  )
}

export default ListTab;
